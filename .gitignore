# === Dependencies ===
node_modules/

# === Build outputs ===
dist/
storybook-static/

# === Caches ===
.vite/
*.tsbuildinfo

# === Generated source (regenerated by tooling) ===
worker-configuration.d.ts
src/client/pages.gen.ts

# === Cloudflare Workers (.dev.vars.example は commit) ===
.wrangler/
.dev.vars
.dev.vars.local

# === Environment / secrets (.env.example は commit) ===
.env
.env.local
.env.*.local
*.local

# === Logs ===
*.log

# === Test artifacts ===
test-results/
playwright-report/
playwright/.cache/
blob-report/
chromatic-archives/
coverage/
*.lcov
.nyc_output/
__diff_output__/

# === vitest-plugin-vis (Storybook 視覚回帰) ===
# `__vis__/<platform>/__baselines__/` のみ commit (Linux baseline で local = WSL / CI = GHA ubuntu)。
# 実行時の actual / diff 出力は git 管理外。
__vis__/local/
__vis__/darwin/
__vis__/win32/
__vis__/*/__results__/
__vis__/*/__diffs__/

# Vitest browser mode が一時的に screenshot を置く dir
.vitest-attachments/

# === Editor / IDE local state ===
.idea/
*.iml
.fleet/
*.swp
*.swo
*~
# VSCode: 共有設定のみ commit (settings.json / extensions.json)
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# === OS noise ===
.DS_Store
Thumbs.db
Desktop.ini

# === AI assistant local-only state (.claude/settings.json と .gemini/settings.json は commit する) ===
.claude/settings.local.json
.claude/.credentials.json
.claude/projects/
.claude/todos/
.claude/shell-snapshots/
.claude/statsig/
.gemini/.tmp/

# === vp commit hooks (husky-style dispatcher; .vite-hooks/pre-commit 本体は commit) ===
.vite-hooks/_/
